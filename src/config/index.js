const passport = require('./passport.js');
const multer = require('./multer.js');
const constants = require('./constants.js');
const socket = require('./socket.js');

module.exports = {
  passport, multer, constants, socket
}